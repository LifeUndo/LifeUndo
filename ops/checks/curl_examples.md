# Curl examples (replace <BASE> if needed)
BASE="https://getlifeundo-lkd7pohn0-alexs-projects-ef5d9b64.vercel.app"

curl -I $BASE/
curl -I $BASE/ru
curl -I $BASE/ru/pricing
curl -I $BASE/ru/download
curl -I $BASE/ok
curl -I $BASE/robots.txt
curl -I $BASE/sitemap.xml
