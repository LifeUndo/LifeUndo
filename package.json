{
  "name": "lifeundo-api",
  "version": "1.0.0",
  "description": "LifeUndo FreeKassa webhook API",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "nodemailer": "^6.9.8"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.21"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}